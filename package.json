{
  "name": "wai-1",
  "version": "1.0.0",
  "description": "Hello World Cloudflare Worker",
  "main": "src/index.ts",
  "scripts": {
    "start": "wrangler dev",
    "deploy": "wrangler deploy --dispatch-namespace testing"
  },
  "dependencies": {
    "wrangler": "^4.0.0"
  }
}
